import React from "react";

function Exit() {
    return (
        <div className="exit">
        <h2>Exit</h2>
        <p>Are you sure you want to exit?</p>
        <button>Yes</button>
        <button>No</button>
        </div>
    );
    }

export default Exit;